<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- SMS: RECEIVE_SMS räcker, READ_SMS behövs ej (innehåll hämtas från broadcast) -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- Boot + bakgrundstjänst -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14: specificera tjänstetyp -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- Larm + skärm -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Android 15+: bakgrundsaktivitet vid larm -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Feature 6: Exact alarms (snooze + repeat) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Feature 5: LED (optional) -->
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <!-- Krävs för att kunna be om undantag från batterioptimering direkt -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.BrandstationLarm"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Visas på låsskärmen när larm triggas -->
        <activity
            android:name=".AlarmActivity"
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:noHistory="true"
            android:theme="@style/Theme.BrandstationLarm.NoActionBar" />

        <activity android:name=".ScheduleActivity" android:exported="false" />
        <activity android:name=".SettingsActivity" android:exported="false" />
        <activity android:name=".PermissionsActivity" android:exported="false" />
        <activity android:name=".LogActivity" android:exported="false" />

        <!--
            foregroundServiceType="remoteMessaging|mediaPlayback":
            - remoteMessaging: mottagning av larmmeddelanden
            - mediaPlayback: uppspelning av larmsignal
            Båda krävs explicit i Android 14+
        -->
        <service
            android:name=".AlarmService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging|mediaPlayback" />

        <!-- SMS-mottagare: prioritet 999 = vi får SMS innan andra appar -->
        <receiver
            android:name=".SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- Autostart vid omstart av telefonen -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Feature 2: Snooze-mottagare -->
        <receiver
            android:name=".SnoozeReceiver"
            android:exported="false" />

        <!-- Feature 8: Hemskärmswidget -->
        <receiver
            android:name=".StatusWidget"
            android:exported="true"
            android:label="Brandstation Status">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

    </application>

</manifest>
